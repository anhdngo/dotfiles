# ~/.profile: executed by the command interpreter for login shells.

# Path modifications
PATH="$HOME/bin:$PATH"
PATH="$HOME/.local/bin:$PATH"
export PATH

# Environment variables
export EDITOR=nvim

# uv installer environment, if present
[ -f "$HOME/.local/bin/env" ] && . "$HOME/.local/bin/env"
